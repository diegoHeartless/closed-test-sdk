<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Optional: improves attaching the latest screenshot on API 33–32 when sharing to Telegram. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" android:minSdkVersion="33" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application>
        <provider
            android:name="io.closedtest.sdk.internal.discovery.ClosedTestDiscoveryProvider"
            android:authorities="${applicationId}.closedtest.discovery"
            android:exported="true" />

        <!-- Implicit intent for discovery without enumerating all installed apps (ProofFlow queries this action). -->
        <activity
            android:name="io.closedtest.sdk.internal.discovery.ClosedTestDiscoveryStubActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="io.closedtest.sdk.DISCOVERY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver
            android:name="io.closedtest.sdk.internal.reminder.LocalDailyReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.closedtest.sdk.action.DAILY_REMINDER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="io.closedtest.sdk.internal.reminder.LocalDailyReminderBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="io.closedtest.sdk.internal.ClosedTestInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>
</manifest>
