<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClosedTestSample">
        <meta-data
            android:name="io.closedtest.sdk.owner_email"
            android:value="sample@example.com" />
        <meta-data
            android:name="io.closedtest.sdk.google_group_url"
            android:value="https://groups.google.com/g/closed-test-sample" />
        <meta-data
            android:name="io.closedtest.sdk.invite_link"
            android:value="https://play.google.com/apps/testing/io.closedtest.sample" />
        <meta-data
            android:name="io.closedtest.sdk.publishable_key"
            android:value="${closedTestPublishableKey}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="bind"
                    android:scheme="closedtest" />
            </intent-filter>
        </activity>
    </application>
</manifest>
